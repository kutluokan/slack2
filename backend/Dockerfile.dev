# Development Dockerfile for backend
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Don't copy source files - we'll mount them as a volume
EXPOSE 4000

CMD ["npm", "run", "dev"]