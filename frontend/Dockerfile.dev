# Development Dockerfile for frontend
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Don't copy source files - we'll mount them as a volume
EXPOSE 3000

CMD ["npm", "run", "dev"]